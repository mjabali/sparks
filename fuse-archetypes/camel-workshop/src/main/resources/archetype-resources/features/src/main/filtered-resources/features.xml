<?xml version="1.0" encoding="UTF-8"?>
<features>
    <feature name="poc-camel-routes" version="${version}">
        <feature version="${camel.version}">camel</feature>
        <feature version="${cxf.version}">cxf</feature>
        <feature version="${camel.version}">camel-cxf</feature>
        <feature version="${camel.version}">camel-jms</feature>
        <feature version='${activemq.version}'>activemq</feature>
        <feature version='${activemq.version}'>activemq-blueprint</feature>
        <feature version='${servicemix.version}'>camel-activemq</feature>
        <feature version="${camel.version}">camel-jaxb</feature>
        <bundle>blueprint:file:etc/activemq-broker.xml</bundle>
        <bundle>mvn:commons-lang/commons-lang/${commons-lang}</bundle>
        <bundle>mvn:${groupId}/routing/${version}</bundle>
    </feature>
</features>